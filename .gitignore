# Build output. vendor/ is deliberately NOT ignored: the release workflow
# builds a fresh checkout, where the ../tdrive replace target does not exist,
# so the dependencies have to travel with the source.
/tdrive-aliyunpan
/dist/
*.test

# Local agent and debugging scratch space.
/.cursor/
